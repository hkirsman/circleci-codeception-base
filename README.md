### Generic CircleCI image for running codeception tests on.

### Build and push

1. Build: `docker build -t wunderio/circleci-codeception-base .`
2. Push: `docker push wunderio/circleci-codeception-base`

